<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myPokeGame.relationMapper.NFileTagRelationMapper">

    <select id="queryFileIdsByTagIds" parameterType="java.util.Map" resultType="long">
        SELECT t.`fileId`
        FROM t_nfile_tag_relation t
        WHERE t.`tagId` IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
        ("a", "b", "c")
        GROUP BY t.`fileId`
        HAVING COUNT(DISTINCT t.`tagId`) = #{count};
    </select>

</mapper>